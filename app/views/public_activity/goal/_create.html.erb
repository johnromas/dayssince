
<% if activity.trackable && !activity.trackable.private? %>
	<strong><%= activity.owner.name %></strong> created a new goal: <strong><%= activity.trackable.title %></strong><br>

<% else %>
  which has since been removed
<% end %>