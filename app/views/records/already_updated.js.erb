var button = $("#up_one_<%=@record.goal.id%>")

button.attr('data-content', 'Already updated today!');
button.popover('show');
button.on('shown.bs.popover', function() {
    setTimeout(function() {
       $(button).popover('hide');
    }, 3000);
});